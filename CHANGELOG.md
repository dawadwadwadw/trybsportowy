# Changelog

Format: `YYYY-MM-DD — <scope> — <one-line summary>` (newest first).

2026-05-19 — Phase 6 complete — UI integration (minimal, surgical — §13). New ui/pro/SyncStatusLine (single 14sp text row, no icons; states per §11.1: token-invalid clickable→Settings, Synchronizuję…, przed chwilą/HH:mm, network/server fail, Brak synchronizacji; observes SyncScheduler flow + mostRecent attempt + isSecretInvalid, 30s refresh). ProDashboard: "Synchronizuj teraz" TopAppBar action with 3s debounce → SyncScheduler.triggerOnce; status line above MirrorChart; per-day score now prefers readiness_computed_cache (algorithmVersion=v1) over local, local-only shown with "~" marker, local/server divergence >1 logged as a §4.1 violation (Log.w, not hidden). ComputedScoreCacheDao.getAll(); ProDashboardScreen gains onOpenSettings (wired in MainActivity to the Phase 3 server-settings route). All new strings in strings.xml. Checkpoint (read-only): ui/ emoji-scan CLEAN, ui/ Polish-literal CLEAN, no control chars, verify-no-secrets CLEAN; legacy ProDashboard Polish literals remain out of scope (§13). installDebug + manual UI walkthrough verified by user in Android Studio.

2026-05-19 — Phase 5 complete — Sync engine + WorkManager. work-runtime-ktx 2.9.1. SyncState enum; ReadinessDao gains query-only observeAll(Flow)/getBySyncState/markSyncState(+Error) (no schema change). Existing single write path (ReadinessRepositoryImpl.saveDailyReadiness) now stamps updatedAt/syncState=PENDING/syncError=null so every create/edit/import is §4.3-correct without rewiring call sites (verified: no UI/Worker touches the DAO directly — §1.5). SyncReadinessRepository (§10.2, renamed to avoid clash with the legacy domain ReadinessRepository interface) is the sole syncState-transition authority. SyncRepository.syncOnce() (§10.3/§4.4/§1.9): canonical-hash idempotency-key reuse, 401→markSecretInvalid+revert PENDING, 207 per-row FAILED, 5xx/IO→PENDING, best-effort readBack into computed cache, 7-day attempt GC. ms↔s epoch boundary isolated in Mappers.kt (local ms, server seconds — convert only at the wire, §4.5; verify vs a real row on device). SyncWorker (AuthFailed=success, no retry); SyncScheduler (6h periodic KEEP + one-time REPLACE, CONNECTED, WM 2.9 Flow status API); scheduled in App.onCreate; syncRepository() manual-DI factory. SyncRepositoryTest (instrumented, in-memory Room + MockWebServer, 5 scenarios incl. idempotency-key reuse + s→ms cache assertion). Build + ./gradlew connectedAndroidTest + live device sync verified by user in Android Studio.

2026-05-19 — Phase 4 complete — Networking layer. Retrofit transport switched to Moshi (codegen via KSP, the project's processor — not kapt); Retrofit 2.9.0→2.11.0; added okhttp core 4.12.0, converter-moshi, moshi, mockwebserver(test). DTOs (DailyReadinessDto, ReadinessRecordDto+RawReadinessDto, ComputedDto, SyncResponseDto+SyncErrorDto, HealthDto→Moshi) mirror §2.2 exactly incl. the POST drain_tags-as-string vs GET drain_tags-as-array asymmetry. ServerApi gains sync (Response<T> for 200/207) + getReadiness; AuthInterceptor (Bearer iff secret) behind a SecretProvider seam so it's unit-testable (SecretsStore implements it, still sole accessor §4.6); NetworkModule rebuilt per §9.6 (AuthInterceptor + redactHeader Authorization/Idempotency-Key + Moshi). canonicalize() implemented now (hand-emitted compact sorted-key JSON byte-identical to Python json.dumps; §4.4) to satisfy CanonicalPayloadTest. AuthInterceptorTest (MockWebServer, 3 cases) + CanonicalPayloadTest (4 cases). INTERNET already in manifest; Tailscale default URL (real LE cert) — no network-security-config needed. Build + ./gradlew test + live "Testuj połączenie" verified by user in Android Studio.

2026-05-19 — Phase 3 complete — Settings + secret provisioning. SecretsStore (EncryptedSharedPreferences AES256, §4.6) exposed as an app singleton; new ServerSettingsScreen + ServerSettingsViewModel (server URL + masked token + save + Testuj połączenie + invalid-token notice), reachable via an entry button added to the existing settings hub (legacy decay/backup screen kept per §13). ServerApi/NetworkModule introduced health-only (Gson converter; Retrofit transport per §1.10) — Phase 4 completes it (sync/getReadiness + AuthInterceptor + redaction; Moshi reserved for §4.4 canonical hashing only). All new strings in strings.xml; scripts/verify-no-secrets.sh + verify-no-polish-literals.sh added (§8.4/§4.7). Note: §4.7 full scan still lists 134 pre-existing legacy literal lines across 14 files — refactoring them is out of scope per §13; all newly added Phase 3 code is CLEAN. Build/device verification by user in Android Studio.

2026-05-19 — Phase 2 complete — Room v3→v4. DailyReadinessEntity gains tz/updatedAt/syncState/syncError (@ColumnInfo defaults matching the migration); new SyncAttemptEntity (sync_attempt, +3 indices) and ComputedScoreCacheEntity (readiness_computed_cache) + DAOs. Hand-written Migration_3_4 (real table name `daily_readiness`; stressCode RETAINED per §1.12 — phase-2.md's drop-stressCode snippet was illustrative and would be destructive). Removed .fallbackToDestructiveMigration() from TrybsportowyApplication (§1.3). exportSchema=true + room.schemaLocation; schemas dir wired as androidTest asset. MigrationTest hand-seeds the v3 schema (no v3 baseline existed) then runMigrationsAndValidate against exported 4.json. Build/instrumented verification by user in Android Studio.

2026-05-19 — Phase 1 complete — Algorithm parity established; 6 fixtures pass (CalculateReadinessUseCaseTest). Added docs/history/ for §1.11 schema backups. (Test run confirmed by user in Android Studio.)

2026-05-15 — Phase 1 (pending verification) — CalculateReadinessUseCase rewritten to conform to §4.1.1: added CNS overload penalty (×0.85 when total_cns+total_body > 6.0 strict) and D-Code (HALF_UP, 1 dp); cnsDrain/bodyDrain removed from the base score (they now only drive overload + D-Code); intermediate math switched to Double. Behavior change: `stressCode` retired from scoring — the column is retained but no longer affects the readiness score. Added AlgorithmVersion.kt (v1), algorithm_v1_fixtures.json (6 fixtures), and the JUnit4 parity harness. Awaiting `./gradlew test` confirmation before Phase 1 is marked complete.

2026-05-15 — Docs — CLAUDE.md installed from the Android Operating Manual and restructured into the lean split: invariants + server contract + cross-cutting rules + algorithm stay in CLAUDE.md; per-phase procedure moved to docs/phases/phase-1..7.md; Master Blueprint archived to docs/MASTER_BLUEPRINT.md (no prior CLAUDE.md existed in the repo).
